+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
PRACTICAL  --> DAY 7 continue ==============================================================================================>
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

REFER DIAGRAM/ARCHITECTURE BEFORE BUILDING PROJECT :-  
https://docs.aws.amazon.com/vpc/latest/userguide/vpc-example-private-subnets-nat.html

4 SUBNET 4 SERVER 
VPC has public subnets and private subnets in 2 availability zones. 
2 SERVERS IN PRIVATE SUBNET WHERE APP WILL BE RUNNING. 
2 SERVERS(bASTION HOST/JUMP SERVER) IN PUBLIC SUBNET THESE WOULD REQUIRE TO CONNECT TO SERVERS IN PRIVATE SUBNET. 
ALL SERVER SHOULD BE IN SAME VPC
AUTOSCALING SHOULD BE APPLIED TO BOTH 2 SERVERS IN PRIVATE SUBNET AS TRAFFIC INCREASES THEN THEY SHOULD BE USED

FIRST CREATE TARGET GROUP AND THEN ONLY IN LB WHICH IS INSIDE EC2 COMPONENTS CAN BE USED

About Project :-->
This project includes how to create VPC in prod environment . 
To improve resiliency you deploy servers in two avalibality zones,by using Autoscaling group and
application LB.
for additional security , you deploy servers in private subnet.
Servers receive request from LB , servers can connect to internet by using NAT gateway,
To imporve resliency NAT gateway deployed in both availability zones. 

Why NAT gateway used ?
-->
NAT gateway is used in public subnet coz if application from private subnet wants to access something from internet then private IP should not be exposed.
so private ip gets masked with NAT IP in public subnet and then content will be accessed .

HOW TO CONNECT TO PRIVATE EC2 INSTANCE OR APP??
==>
USER LOCAL SYSTEM (with private key) ==> JUMP SERVER (configur private key in this server as well)  ==> PRIVATE EC2 APP
KEY PAIR COPY FROM LOCAL INSIDE JUMP SERVER AND FROM THEIR KEY PAIR WE CONNECT TO PROD APP IN PRIVATE EC2.


STEPS :- 

1) CREATE VPC :

- default cidr
- 2 AZ and NAT at each zone
- 2 private 2 public subnet 
- keep all other options as it is.

2) CREATE Autoscaling group :-   // should be created for app servers in 2 private subnet if cpu utlization got above 80 percent then instances should be increasedd.

- but before creating autoscaling you needs to create launch template where instance related configs are defined.

- in laucnh template , use ubuntu AMI , instance type free tier t2 micro , create key value pair,  Add SG where ssh should be allowed for everyone
  tcp port 8000 also all traffic should be allowed so here in 8000 we will run python app
Note :- select VPC recent one which got created  . 

- Now once again create autoscaling group with above launch template name --> in choose instance select 2 private subnets of our VPC --> group sizing and limit select desired capacity as 2 instances
  min capacity 1 and max 4 . 

Now after creation check ec2 instances got created or not and check subnets of it as different availablity zone should be assigned to each instance. 
u can see public address will not be there for both so to access this app servers bastion host or jump server should be used which should be in public subnet.

3) Create BASTION host or jump server :- To keep app server secured no one can login into private instances directly so bastion host should be used to do so.

While creating host make sure vpc should be ours only not default one and public subnet assign while creation.
Keep all setting as it is for creating new instance like allow only ssh from all traffic.
From this user will ssh to bastion host and bastion host will ssh to private instance .

in gitbash add below command to share the private key to ubuntu server :- 
scp -i /c/Users/Prajwal/OneDrive/Desktop/programs/AWS-new-projects/pvk-aws.pem /c/Users/Prajwal/OneDrive/Desktop/programs/AWS-new-projects/pvk-aws.pem ubuntu@3.110.219.3:/home/ubuntu/

on ubuntu bastion host :- /home/ubuntu
chmod 400 "pvk-aws.pem"
ssh -i "pvk-aws.pem" ubuntu@10.0.130.163

u are now connected on private ec2 instance !!

Now after connection run python on 8000 port on server :- 


4) Create LB . client --> VPC --> IG --> LB --> private IP 

- Go to ec2 --> LB --> create LB  --> application LB
- like for autoscaling group  launch teamplate was compulsory , now here for LB target groups are important while creating LB









